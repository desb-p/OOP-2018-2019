package ie.dit;

import processing.core.PApplet;

// extends - subclass of PApplet(processing library)
public class HelloProcessing extends PApplet
{	
	public void settings()
	{
		// size of the drawing window box.
		size(500, 500);
	}

	public void setup() 
	{
		
	}

	// int x = 0; // 4 bytes
	// int xx = 2;
	// float f = 0.0f; // 4bytes
	// char c = 'h'; // Unicode.
	// String s = "Hello";
	// boolean b = false; // boolean b = (1 == 0);
	// double d = 0; //  8 bytes.
	


	public void draw() 
	{

		//background(0, 0, 255); //
		//stroke(255, 0, 0); 
		// start of x and y, end of x and y.
		// line(10, 10, 100, 100);	 // x1, y1, x2, y2
		//point(50,50); // x, y 
		//ellipse(100,200,50,50);
		//ellipse(100,200,50,150);  //cx, cy, w, h (circular shape)
		//fill(0, 255, 0); 
		// rect(50, 10,200); // top left x , top left  , w , h
		//triangle(10,10,100,90,200,200);
		// noStroke();


		// x = 0;
		// x = x + 10 ; // x+=10;
		// x = x / xx;
		// x /=xx;
		// x *=10;
		// s += "World";
		
		// background(0);
		// noStroke();
		// fill(255);
		// ellipse(mouseX, mouseY, 50, 50);

		// x ++; 

	}	

	

	


}
